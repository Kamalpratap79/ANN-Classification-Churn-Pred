# ANN-Classification-Churn-Pred

A machine learning web application that predicts whether bank customers will leave using an Artificial Neural Network. Input customer data (age, credit score, balance, account activity) and get instant churn probability predictions.

## Quick Start

```bash
pip install -r requirements.txt
streamlit run app.py
